import { DataHighlightPipe } from './data-highlight.pipe';

describe('DataHighlightPipe', () => {
  it('create an instance', () => {
    const pipe = new DataHighlightPipe();
    expect(pipe).toBeTruthy();
  });
});
